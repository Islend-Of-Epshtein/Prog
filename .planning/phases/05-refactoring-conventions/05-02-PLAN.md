---
phase: 05-refactoring-conventions
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - L4S4-Maven/src/main/java/base/Server.java
  - L4S4-Maven/src/main/java/base/Client.java
  - L4S4-Maven/src/main/java/task1/FileServer.java
  - L4S4-Maven/src/main/java/task1/ClientRequest.java
  - L4S4-Maven/src/main/java/gui/task1/InputAdressFrame.java
autonomous: true
requirements:
  - QR-001
must_haves:
  truths:
    - "All methods in Server.java use camelCase"
    - "All methods in Client.java use camelCase"
    - "All callers updated to use new method names"
    - "Project compiles after method renaming"
  artifacts:
    - path: "L4S4-Maven/src/main/java/base/Server.java"
      provides: "Renamed methods"
      contains: "public.*write\\(|public.*read\\(|public.*accept\\("
    - path: "L4S4-Maven/src/main/java/base/Client.java"
      provides: "Renamed methods"
      contains: "public.*write\\(|public.*read\\("
  key_links:
    - from: "task1/FileServer.java"
      to: "base/Server.java"
      via: "extends Server, overrides methods"
    - from: "gui/task1/InputAdressFrame.java"
      to: "base/Client.java"
      via: "calls Write, Off methods"
---

<objective>
Rename all methods in Base classes (Server.java, Client.java) from PascalCase to camelCase to comply with Java Naming Conventions (QR-001).

Purpose: Java methods must use camelCase. Current methods (Write, Read, Accept, Off, IsBound, GetAddress, GetPort, GetLocalPort, GetSocket) violate this standard.
Output: All methods renamed, all callers updated.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Methods to rename in Server.java -->
From Base/Server.java (now base/Server.java):
- Write(String str) → write(String str)
- Write(String str, boolean log) → write(String str, boolean log)
- Read() → read()
- Accept() → accept()
- Off() → close() (better semantic name)
- IsBound() → isBound()
- GetAddress() → getAddress()
- GetPort() → getPort()
- GetLocalPort() → getLocalPort()
- GetSocket() → getSocket()

From Base/Client.java (now base/Client.java):
- Write(String str) → write(String str)
- Write(String str, boolean log) → write(String str, boolean log)
- Read() → read()
- Off() → close()
- IsBound() → isBound()
- GetAddress() → getAddress()
- GetPort() → getPort()
- GetLocalPort() → getLocalPort()
- GetSocket() → getSocket()

Callers to update:
- task1/FileServer.java: Accept(), Off(), Write(), Read(), IsBound()
- task1/ClientRequest.java: Off(), Write()
- gui/task1/InputAdressFrame.java: Off(), Write()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename methods in Server.java</name>
  <files>L4S4-Maven/src/main/java/base/Server.java</files>
  <action>
Rename all methods in Server.java from PascalCase to camelCase:
1. `public String Write(String str)` → `public String write(String str)`
2. `public void Write(String str, boolean log)` → `public void write(String str, boolean log)`
3. `public String Read()` → `public String read()`
4. `public void Accept()` → `public void accept()`
5. `public void Off()` → `public void close()` (use close() as it's the standard name for resource cleanup)
6. `public boolean IsBound()` → `public boolean isBound()`
7. `public String GetAddress()` → `public String getAddress()`
8. `public int GetPort()` → `public int getPort()`
9. `public int GetLocalPort()` → `public int getLocalPort()`
10. `public Socket GetSocket()` → `public Socket getSocket()`

Also update the non-standard `///` comments to proper JavaDoc `/** */` format.
  </action>
  <verify>
    <automated>grep -E "public.*[A-Z][a-z]+\\(" L4S4-Maven/src/main/java/base/Server.java | wc -l</automated>
    Expected: 0 (no PascalCase public methods)
  </verify>
  <done>All methods in Server.java renamed to camelCase.</done>
</task>

<task type="auto">
  <name>Task 2: Rename methods in Client.java</name>
  <files>L4S4-Maven/src/main/java/base/Client.java</files>
  <action>
Rename all methods in Client.java from PascalCase to camelCase:
1. `public String Write(String str)` → `public String write(String str)`
2. `public void Write(String str, boolean log)` → `public void write(String str, boolean log)`
3. `public String Read()` → `public String read()`
4. `public void Off()` → `public void close()`
5. `public boolean IsBound()` → `public boolean isBound()`
6. `public String GetAddress()` → `public String getAddress()`
7. `public int GetPort()` → `public int getPort()`
8. `public int GetLocalPort()` → `public int getLocalPort()`
9. `public Socket GetSocket()` → `public Socket getSocket()`

Also update the non-standard `///` comments to proper JavaDoc `/** */` format.
  </action>
  <verify>
    <automated>grep -E "public.*[A-Z][a-z]+\\(" L4S4-Maven/src/main/java/base/Client.java | wc -l</automated>
    Expected: 0 (no PascalCase public methods)
  </verify>
  <done>All methods in Client.java renamed to camelCase.</done>
</task>

<task type="auto">
  <name>Task 3: Update all callers of renamed methods</name>
  <files>
    - L4S4-Maven/src/main/java/task1/FileServer.java
    - L4S4-Maven/src/main/java/task1/ClientRequest.java
    - L4S4-Maven/src/main/java/gui/task1/InputAdressFrame.java
  </files>
  <action>
Update all callers to use the new method names:

In task1/FileServer.java:
- `super.Accept()` → `super.accept()`
- `super.Off()` → `super.close()`
- `super.Write()` → `super.write()`
- `this.IsBound()` → `this.isBound()`
- `Read()` → `read()`
- Override annotation: `@Override public void Accept()` → `@Override public void accept()`
- Override annotation: `@Override public void Off()` → `@Override public void close()`

In task1/ClientRequest.java:
- `Off()` → `close()`
- `Write()` → `write()`

In gui/task1/InputAdressFrame.java:
- `server.Off()` → `server.close()`
- `client.Off()` → `client.close()`
- `client.Write()` → `client.write()`
- `server.Accept()` → `server.accept()`

After updating, run `mvn compile` to verify.
  </action>
  <verify>
    <automated>cd L4S4-Maven && mvn compile -q 2>&1 | grep -c "ERROR"</automated>
    Expected: 0 errors (compilation succeeds)
  </verify>
  <done>All callers updated, project compiles successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This is a refactoring task with no security implications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | N/A | Method renaming | accept | No security impact - pure refactoring |
</threat_model>

<verification>
1. No PascalCase public methods in Server.java
2. No PascalCase public methods in Client.java
3. All callers use new method names
4. `mvn compile` passes without errors
</verification>

<success_criteria>
- [ ] Server.java methods: write(), read(), accept(), close(), isBound(), getAddress(), getPort(), getLocalPort(), getSocket()
- [ ] Client.java methods: write(), read(), close(), isBound(), getAddress(), getPort(), getLocalPort(), getSocket()
- [ ] FileServer.java: overrides accept() and close()
- [ ] `mvn compile` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-refactoring-conventions/05-02-SUMMARY.md`
</output>
