---
phase: 05-refactoring-conventions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - L4S4-Maven/src/main/java/Base/*.java
  - L4S4-Maven/src/main/java/GUI/**/*.java
  - L4S4-Maven/src/main/java/Task1/*.java
  - L4S4-Maven/src/main/java/Task2/*.java
  - L4S4-Maven/src/main/java/Task3/*.java
  - L4S4-Maven/src/main/java/SelectTaskMenu.java
autonomous: true
requirements:
  - QR-001
must_haves:
  truths:
    - "All package declarations use lowercase names"
    - "All import statements reference lowercase package names"
    - "Project compiles after package renaming"
  artifacts:
    - path: "L4S4-Maven/src/main/java/base/"
      provides: "Renamed Base package"
    - path: "L4S4-Maven/src/main/java/gui/"
      provides: "Renamed GUI package"
    - path: "L4S4-Maven/src/main/java/task1/"
      provides: "Renamed Task1 package"
    - path: "L4S4-Maven/src/main/java/task2/"
      provides: "Renamed Task2 package"
    - path: "L4S4-Maven/src/main/java/task3/"
      provides: "Renamed Task3 package"
  key_links:
    - from: "All Java files"
      to: "Package declarations"
      via: "package statement at line 1"
      pattern: "^package\\s+[a-z]+"
---

<objective>
Rename all packages from PascalCase to lowercase to comply with Java Naming Conventions (QR-001).

Purpose: Java package names must be lowercase by convention. Current packages (Base, GUI, Task1, Task2, Task3) violate this standard.
Output: All directories renamed, all package declarations and imports updated.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

<interfaces>
<!-- Package structure to rename -->
Packages to rename:
- Base → base (contains Server.java, Client.java)
- GUI → gui (contains Task1/, Task2/, Task3/ subdirs)
- Task1 → task1 (contains FileServer.java, ClientRequest.java, Cortege.java)
- Task2 → task2 (contains Controller.java, Dispatcher.java, ProcessData.java, etc.)
- Task3 → task3 (contains Controller.java, Dispatcher.java)

Files affected:
- All Java files in L4S4-Maven/src/main/java/
- SelectTaskMenu.java (root level, imports from GUI, Task1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package directories</name>
  <files>
    - L4S4-Maven/src/main/java/Base/ → base/
    - L4S4-Maven/src/main/java/GUI/ → gui/
    - L4S4-Maven/src/main/java/Task1/ → task1/
    - L4S4-Maven/src/main/java/Task2/ → task2/
    - L4S4-Maven/src/main/java/Task3/ → task3/
  </files>
  <action>
Rename all package directories from PascalCase to lowercase:
1. Rename `L4S4-Maven/src/main/java/Base/` to `L4S4-Maven/src/main/java/base/`
2. Rename `L4S4-Maven/src/main/java/GUI/` to `L4S4-Maven/src/main/java/gui/`
3. Rename `L4S4-Maven/src/main/java/Task1/` to `L4S4-Maven/src/main/java/task1/`
4. Rename `L4S4-Maven/src/main/java/Task2/` to `L4S4-Maven/src/main/java/task2/`
5. Rename `L4S4-Maven/src/main/java/Task3/` to `L4S4-Maven/src/main/java/task3/`

Use `git mv` to preserve history. The GUI subdirectories (Task1, Task2, Task3) will be renamed in step 2 to gui/task1, gui/task2, gui/task3.
  </action>
  <verify>
    <automated>ls L4S4-Maven/src/main/java/ | grep -E "^[a-z]+$" | wc -l</automated>
    Expected: 5 directories (base, gui, task1, task2, task3, SelectTaskMenu.java)
  </verify>
  <done>All five package directories renamed to lowercase, git history preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update package declarations</name>
  <files>
    - L4S4-Maven/src/main/java/base/*.java
    - L4S4-Maven/src/main/java/gui/**/*.java
    - L4S4-Maven/src/main/java/task1/*.java
    - L4S4-Maven/src/main/java/task2/*.java
    - L4S4-Maven/src/main/java/task3/*.java
    - L4S4-Maven/src/main/java/SelectTaskMenu.java
  </files>
  <action>
Update all package declarations from PascalCase to lowercase:
- `package Base;` → `package base;`
- `package GUI.Task1;` → `package gui.task1;`
- `package GUI.Task2;` → `package gui.task2;`
- `package GUI.Task3;` → `package gui.task3;`
- `package Task1;` → `package task1;`
- `package Task2;` → `package task2;`
- `package Task3;` → `package task3;`

Update all import statements:
- `import Base.Server;` → `import base.Server;`
- `import Base.Client;` → `import base.Client;`
- `import GUI.Task1.InputAddressFrame;` → `import gui.task1.InputAdressFrame;`
- `import Task1.*;` → `import task1.*;`
- etc.

Also rename GUI subdirectories:
- `gui/Task1/` → `gui/task1/`
- `gui/Task2/` → `gui/task2/`
- `gui/Task3/` → `gui/task3/`
  </action>
  <verify>
    <automated>grep -r "^package [A-Z]" L4S4-Maven/src/main/java/ 2>/dev/null | wc -l</automated>
    Expected: 0 (no PascalCase packages remaining)
  </verify>
  <done>All package declarations and imports updated to lowercase.</done>
</task>

<task type="auto">
  <name>Task 3: Verify compilation after package rename</name>
  <files>L4S4-Maven/pom.xml</files>
  <action>
Run Maven compile to verify all package renames are correct:
1. `mvn clean compile` from L4S4-Maven directory
2. Fix any remaining references to old package names
3. Ensure no compilation errors

If compilation fails, search for remaining PascalCase package references:
```bash
grep -r "import [A-Z]" L4S4-Maven/src/main/java/
grep -r "package [A-Z]" L4S4-Maven/src/main/java/
```
  </action>
  <verify>
    <automated>cd L4S4-Maven && mvn compile -q 2>&1 | grep -c "ERROR"</automated>
    Expected: 0 errors
  </verify>
  <done>Project compiles successfully with lowercase package names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This is a refactoring task with no security implications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | N/A | Package renaming | accept | No security impact - pure refactoring |
</threat_model>

<verification>
1. All directories in `L4S4-Maven/src/main/java/` are lowercase
2. All package declarations are lowercase
3. All imports reference lowercase packages
4. `mvn compile` passes without errors
5. No references to old PascalCase package names remain
</verification>

<success_criteria>
- [ ] Directory structure: base/, gui/, task1/, task2/, task3/ (all lowercase)
- [ ] All `package` statements use lowercase
- [ ] All `import` statements use lowercase
- [ ] `mvn compile` succeeds
- [ ] Git history preserved (used `git mv`)
</success_criteria>

<output>
After completion, create `.planning/phases/05-refactoring-conventions/05-01-SUMMARY.md`
</output>
