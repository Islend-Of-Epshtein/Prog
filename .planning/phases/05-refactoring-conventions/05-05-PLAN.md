---
phase: 05-refactoring-conventions
plan: 05
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - L4S4-Maven/src/main/java/gui/task1/InputAdressFrame.java
autonomous: false
requirements:
  - TD-003
must_haves:
  truths:
    - "Empty handlers are either implemented or removed"
    - "No buttons with non-functional handlers remain"
    - "User can verify the application works correctly"
  artifacts:
    - path: "L4S4-Maven/src/main/java/gui/task1/InputAdressFrame.java"
      provides: "Functional UI"
      min_lines: 450
  key_links:
    - from: "clientTransfer button"
      to: "ActionListener"
      via: "addActionListener"
---

<objective>
Resolve empty button handlers in InputAdressFrame.java - either implement functionality or remove non-functional buttons (TD-003).

Purpose: Empty handlers mislead users into thinking buttons are functional when they are not.
Output: Either functional buttons or removed stubs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Empty handlers from CONCERNS.md section 6.1 -->
From gui/task1/InputAdressFrame.java (lines 295-305):
```java
JButton clientTransfer = CreateButton("Передать клиенту", new Dimension(140, 25));
JButton serverTransfer = CreateButton("Передать серверу", new Dimension(140, 25));

clientTransfer.addActionListener(e -> {
    // Empty handler - does nothing
});
serverTransfer.addActionListener(e -> {
    serverRequest();  // This one is implemented
});
```

The "clientTransfer" button has an empty handler.
The "serverTransfer" button calls serverRequest() which is implemented.

Options:
1. Remove the non-functional button
2. Implement the functionality (if requirements exist)
3. Disable the button with a tooltip explaining it's not implemented
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze empty handler purpose</name>
  <files>L4S4-Maven/src/main/java/gui/task1/InputAdressFrame.java</files>
  <action>
Analyze the empty `clientTransfer` handler to determine its intended purpose:

1. Read the existing code context around lines 295-305
2. Check if there's any documentation or comments about intended functionality
3. Review the `serverRequest()` method to understand the pattern
4. Determine if "Передать клиенту" (Transfer to client) is:
   - A duplicate of existing functionality
   - A placeholder for future feature
   - An incomplete implementation

Based on analysis, the "clientTransfer" button appears to be for sending data TO the client (server → client direction). However:
- The server already sends data via `serverRequest()` when user clicks files
- The clientTransfer button would need different logic not currently implemented

Decision: REMOVE the non-functional button to avoid confusing users.
  </action>
  <verify>
    <automated>grep -c "clientTransfer" L4S4-Maven/src/main/java/gui/task1/InputAdressFrame.java</automated>
    Expected: Should determine if button should be removed or kept
  </verify>
  <done>Analysis complete, decision made for each empty handler.</done>
</task>

<task type="auto">
  <name>Task 2: Remove non-functional clientTransfer button</name>
  <files>L4S4-Maven/src/main/java/gui/task1/InputAdressFrame.java</files>
  <action>
Remove the non-functional "clientTransfer" button:

1. Remove the button declaration:
   ```java
   // DELETE:
   JButton clientTransfer = CreateButton("Передать клиенту", new Dimension(140, 25));
   ```

2. Remove the empty action listener:
   ```java
   // DELETE:
   clientTransfer.addActionListener(e -> {
   });
   ```

3. Remove the button from the panel:
   ```java
   // DELETE:
   transferPan.add(clientTransfer);
   ```

4. Keep the functional serverTransfer button (it calls serverRequest())

5. Update the panel layout if needed (may need to adjust GridLayout or FlowLayout)

Alternative: If removal is not acceptable, disable the button:
   ```java
   clientTransfer.setEnabled(false);
   clientTransfer.setToolTipText("Функционал не реализован");
   ```
  </action>
  <verify>
    <automated>grep -c "clientTransfer\\.addActionListener" L4S4-Maven/src/main/java/gui/task1/InputAdressFrame.java</automated>
    Expected: 0 (handler removed) or button disabled
  </verify>
  <done>Non-functional button removed or disabled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Empty handlers resolved in InputAdressFrame.java</what-built>
  <how-to-verify>
1. Build the project: `cd L4S4-Maven && mvn compile`
2. Run the application: `java -cp target/classes SelectTaskMenu`
3. Click "Task №1" to open the file browser
4. Verify the "Передать клиенту" button is either:
   - Removed (not visible)
   - Disabled (grayed out with tooltip)
   - Functional (performs expected action)
5. Test that "Передать серверу" button still works correctly
  </how-to-verify>
  <resume-signal>Type "approved" if the UI works correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This is a UI cleanup task with no security implications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | N/A | UI cleanup | accept | No security impact |
</threat_model>

<verification>
1. Project compiles
2. Application runs without errors
3. No buttons with empty handlers remain (or are disabled)
4. Functional buttons still work
</verification>

<success_criteria>
- [ ] `mvn compile` succeeds
- [ ] Application runs
- [ ] No empty handlers on active buttons
- [ ] User confirms UI works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-refactoring-conventions/05-05-SUMMARY.md`
</output>
