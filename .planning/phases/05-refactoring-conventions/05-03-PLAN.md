---
phase: 05-refactoring-conventions
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - L4S4-Maven/src/main/java/SelectTaskMenu.java
  - L4S4-Maven/src/main/java/gui/task1/InputAdressFrame.java
  - L4S4-Maven/src/main/java/gui/task3/Launcher.java
autonomous: true
requirements:
  - QR-001
must_haves:
  truths:
    - "All methods in SelectTaskMenu.java use camelCase"
    - "All methods in InputAdressFrame.java use camelCase"
    - "Project compiles after method renaming"
  artifacts:
    - path: "L4S4-Maven/src/main/java/SelectTaskMenu.java"
      provides: "Renamed static methods"
      contains: "static void initFrame|static JButton createButton"
    - path: "L4S4-Maven/src/main/java/gui/task1/InputAdressFrame.java"
      provides: "Renamed methods"
      contains: "public static void run|private static JButton createButton"
  key_links:
    - from: "SelectTaskMenu.java"
      to: "gui.task1.InputAdressFrame"
      via: "calls Run() method"
---

<objective>
Rename all methods in GUI classes from PascalCase to camelCase to comply with Java Naming Conventions (QR-001).

Purpose: Java methods must use camelCase. Current methods (InitFrame, CreateButton, Run) violate this standard.
Output: All GUI methods renamed, all callers updated.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Methods to rename in SelectTaskMenu.java -->
From SelectTaskMenu.java:
- InitFrame() → initFrame()
- CreateButton(String text) → createButton(String text)

<!-- Methods to rename in InputAdressFrame.java -->
From gui/task1/InputAdressFrame.java:
- Run() → run()
- CreateButton(String text, Dimension size) → createButton(String text, Dimension size)

<!-- Methods in Launcher.java -->
From gui/task3/Launcher.java:
- run() - already lowercase (check if correct)

Callers:
- SelectTaskMenu.main() calls InitFrame()
- SelectTaskMenu.InitFrame() calls CreateButton()
- SelectTaskMenu button1 action: GUI.Task1.InputAdressFrame.Run()
- InputAdressFrame.initElements() calls CreateButton()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename methods in SelectTaskMenu.java</name>
  <files>L4S4-Maven/src/main/java/SelectTaskMenu.java</files>
  <action>
Rename all methods in SelectTaskMenu.java from PascalCase to camelCase:
1. `static void InitFrame()` → `static void initFrame()`
2. `public static JButton CreateButton(String text)` → `public static JButton createButton(String text)`

Update the call in main():
- `InitFrame()` → `initFrame()`

Also update the non-standard `///` comments to proper JavaDoc `/** */` format.
  </action>
  <verify>
    <automated>grep -E "(static|public).*[A-Z][a-z]+\\(" L4S4-Maven/src/main/java/SelectTaskMenu.java | wc -l</automated>
    Expected: 0 (no PascalCase methods)
  </verify>
  <done>All methods in SelectTaskMenu.java renamed to camelCase.</done>
</task>

<task type="auto">
  <name>Task 2: Rename methods in InputAdressFrame.java</name>
  <files>L4S4-Maven/src/main/java/gui/task1/InputAdressFrame.java</files>
  <action>
Rename all methods in InputAdressFrame.java from PascalCase to camelCase:
1. `public static void Run()` → `public static void run()`
2. `public static JButton CreateButton(String text, Dimension size)` → `private static JButton createButton(String text, Dimension size)`

Note: CreateButton should be private as it's only used within this class.

Update all internal calls:
- `CreateButton("...", new Dimension(...))` → `createButton("...", new Dimension(...))`
- The method is already called via `gui.task1.InputAdressFrame.Run()` from SelectTaskMenu (update to `run()`).
  </action>
  <verify>
    <automated>grep -E "public static [A-Z][a-z]+\\(" L4S4-Maven/src/main/java/gui/task1/InputAdressFrame.java | wc -l</automated>
    Expected: 0 (no PascalCase public static methods)
  </verify>
  <done>All methods in InputAdressFrame.java renamed to camelCase.</done>
</task>

<task type="auto">
  <name>Task 3: Update callers and verify compilation</name>
  <files>
    - L4S4-Maven/src/main/java/SelectTaskMenu.java
    - L4S4-Maven/src/main/java/gui/task3/Launcher.java
  </files>
  <action>
Update all callers to use the new method names:

In SelectTaskMenu.java (already updated in Task 1):
- Confirm `initFrame()` and `createButton()` are used

Update the action listener that calls InputAdressFrame:
- `GUI.Task1.InputAdressFrame.Run()` → `gui.task1.InputAdressFrame.run()`

In gui/task3/Launcher.java:
- Check if `run()` method exists and is correctly named (should already be lowercase)

Run `mvn compile` to verify all changes compile correctly.
  </action>
  <verify>
    <automated>cd L4S4-Maven && mvn compile -q 2>&1 | grep -c "ERROR"</automated>
    Expected: 0 errors (compilation succeeds)
  </verify>
  <done>All callers updated, project compiles successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This is a refactoring task with no security implications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | N/A | Method renaming | accept | No security impact - pure refactoring |
</threat_model>

<verification>
1. No PascalCase methods in SelectTaskMenu.java
2. No PascalCase public static methods in InputAdressFrame.java
3. All callers use new method names
4. `mvn compile` passes without errors
</verification>

<success_criteria>
- [ ] SelectTaskMenu.java: initFrame(), createButton()
- [ ] InputAdressFrame.java: run(), createButton() (private)
- [ ] `mvn compile` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-refactoring-conventions/05-03-SUMMARY.md`
</output>
