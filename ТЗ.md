Лабораторная работа 3

Список задач.

1. Разработать класс MenuModel реализующий логику сборки дерева. (.dll)
Должен содержать класс для работы с файлом menu.txt. Структура файла:
# №_Иерархии Имя_компонента Null/Имя_метода
# В методичке в примере menu.txt ошибка, лишняя цифра после имени компонента ( 0 Name ->0 methodName )
    Файл обрабатывается в конструкторе.
    private-класс Element содержит данные об одной строке menu.txt.
    Конструктор заполняет массив элементов Element.
MenuFrame должна содержать метод для возвращения количества элементов в текущей иерархии
и геттеры для прав доступа, имя метода текущего элемента, названия и номера иерархии.

структура классов :

public class MenuModel{
    List<Element> elements;
    private class Element { . . . }
    . . . 
}

2. МainFrame - отображение меню. (тестовая программа)
-осн. на раннем связывании (-> dll подгружается на этапе загрузки ))
Создает MenuBar на основе menu.txt, используя бибоиотеку для работы с файлом MenuFrame
Реализует обработчики нажатий мыши для имен методов из MenuFrame.

# Необходимо два варианта тестовой программы - с ранним и поздним связыванием.
    1. Неявное свзывание - загрузка dll на момент запуска программы.
        -обработка наличия dll невозможна. без dll нельзя продолжить выполнение
    2. Явное связывание - подгрузка dll во время выполнения с помощью методов (exmpl. AssembleyLoad())
        -возможна обработка ошибки отсутствия dll

3. LogInFrame - окно авторизации. Использует файл users.txt     (.dll)
После логина :
            1) Изменить файл menu.txt выгрузив недоступные 
            2) загрузить объекты обратно при завершении работы
# Таким образом MainFrame не зависит от LogInFrame и .dll можно либо, добавлять, либо нет.
